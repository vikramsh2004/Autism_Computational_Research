# Autism_Computational_Research

## Basic Hodgkin-Huxley script

Run the script:

```bash
python3 hodgkin_huxley_basic.py
```

It simulates a classic Hodgkin-Huxley neuron with a square current pulse, prints a quick summary (spike count and voltage range), and writes `hh_trace.csv` with time series data.